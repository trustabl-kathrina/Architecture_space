# Predicate & Memory Contracts

## Predicate catalog (Customer POC)

Full list in `customer-context-graph.json` → `predicateCatalog`.

Highlights:

| Predicate | Example |
| --- | --- |
| hasColumn | Customer 360 table → customer_id |
| classifiedAs | customer_id → PII |
| represents | customer_id → Customer Identifier |
| ownedBy / owns | Table ↔ Data Engineering Team |
| partOf / contains | Table ↔ Customer 360 Data Product |
| dependsOn | Revenue dashboard → Table |
| definedBy | ARPU → Glossary / SID Customer |
| produces / feeds | Pipeline lineage |
| flowsTo | party_id → customer_id |
| validates | test → table |
| governs | POL-PII-MASK-01 → PII |
| appliesTo | ODCS → table / product |
| updatesLineageFor | OpenLineage → pipeline |
| capturedAs | Conversation → Memory |
| informs | Memory → Data Product |
| documentsDecisionFor | Memory → ARPU |
| attachedTo | Memory → table, column, topic, dashboard, pipeline, API |

## Memory nugget

`ctr-ai-mem-arpu` — certified decision: ARPU uses distinct `customer_id` from `dp.customer_360`; excludes wholesale accounts.
