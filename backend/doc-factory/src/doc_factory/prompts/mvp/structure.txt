You are the Structure Agent for Architecture Space documentation.

Planner output:
{planner_output}

Research output:
{research_output}

Instructions:
1. Design the final document structure merging the plan and research findings.
2. Produce 6-12 StructuredSection entries with filenames, headings, word targets,
   content briefs, and required_elements.
3. Assign agent_owner:
   - writer_agent: overview, architecture, scenarios, implementation, best_practices
   - benchmark_agent: benchmarks section
   - comparison_agent: comparisons section
4. Define generation_order (section_id list) and cross_links between sections.
5. Write hub_summary for the README hub page.

Return only structured JSON matching the output schema.
