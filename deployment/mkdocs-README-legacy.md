# Tools

Supporting tooling for the KEW monorepo (not part of the workbench runtime).

| Path | Purpose |
|------|---------|
| [`docs/`](docs/) | MkDocs site config, document templates, corpus validation and maintenance scripts |

KEW dev scripts (`setup`, `dev-api`, `dev-web`, `test-api`) live in [`../scripts/`](../scripts/).
